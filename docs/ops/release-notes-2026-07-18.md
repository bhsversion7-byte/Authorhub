# 2026-07-18 UI stability release notes

- Restored fixed, user-resizable compact writing cards with internal scroll and local height persistence.
- Restored reading-settings paper texture and tightened the primary settings rows.
- Unified reading settings typography across labels, controls, icons, and floating tools.
- Added paper-fibre texture to scratchpad mind maps without adding notebook rules.
- Corrected onboarding targets for both floating tools, sharing, and the outline focus editor.
- Hardened main-pair relationship labels to use the same fixed red as their line, and corrected graph dragging while zoomed.
- Production configuration check: Vercel has both Turnstile variables and the verification endpoint returns a configured response. Supabase leaked-password protection remains a provider-console action.
